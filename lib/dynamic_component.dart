library dynamic_component;

export 'src/dynamic_component.dart';
export 'src/if_or_not.dart';

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
}
