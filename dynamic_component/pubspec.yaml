name: dynamic_component
description: Build your Dynamic components that support simple logic and variable.
version: 0.2.0+2
homepage: https://github.com/TshineZheng/dynamic_component

environment:
  sdk: ">=2.13.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dynamic_widget: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
